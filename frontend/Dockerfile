# Build stage
FROM node:20-alpine AS build
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build -- --configuration production

# Nginx serve stage
FROM nginx:1.27-alpine
COPY --from=build /app/dist/user-frontend /usr/share/nginx/html
EXPOSE 80
